[project]
name = "powerline-claude-code"
version = "0.0.2"
description = "Powerline segments for Claude Code: tmux attention indicator + rolling token usage."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.urls]
Homepage = "https://github.com/cwood/powerline-claude-code"
