[project]
name = "powerline-claude-code"
version = "0.0.1"
description = "Powerline segment that highlights tmux windows where Claude Code is waiting on you."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.urls]
Homepage = "https://github.com/cwood/powerline-claude-code"
